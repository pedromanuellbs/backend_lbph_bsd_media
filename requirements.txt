Flask==2.2.5
# OpenCV: Gunakan versi yang saya tahu sudah diuji di lingkungan Docker.
# 4.5.5.64 sering lebih stabil untuk CPU daripada versi terbaru yang sangat baru.
opencv-python==4.5.5.64

numpy==1.26.4
Pillow==10.2.0

# PyTorch (CPU Only) - Versi stabil dengan index-url yang wajib
# Ini adalah versi yang dikenal berfungsi baik untuk CPU di Python 3.10
torch==1.13.1+cpu --index-url https://download.pytorch.org/whl/cpu
torchvision==0.14.1+cpu --index-url https://download.pytorch.org/whl/cpu

facenet-pytorch==2.5.2 # Versi yang kompatibel dengan PyTorch 1.13.1

scikit-learn==1.4.1
requests==2.31.0 # Versi stabil
gunicorn==22.0.0
gevent==22.10.2
firebase-admin==6.4.0
google-api-python-client==2.128.0